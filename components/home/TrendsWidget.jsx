const TrendsWidget = ({ data }) => {
//  

  return (
    <div className='trends-widget'>
      {/* <h2>Trends Overview</h2>
      <ul>
        <li>Visits: {visits ? visits.summary : 'Loading...'}</li>
        <li>Locations: {locations ? locations.summary : 'Loading...'}</li>
        <li>Discovery: {discovery ? discovery.summary : 'Loading...'}</li>
        <li>Reasons: {reasons ? reasons.summary : 'Loading...'}</li>
        <li>Age Groups: {ageGroups ? ageGroups.summary : 'Loading...'}</li>
        <li>Gender: {gender ? gender.summary : 'Loading...'}</li>
      </ul> */}
    </div>
  );
};

export default TrendsWidget;
